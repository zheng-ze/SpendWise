name: sync
description: Pure-Dart SpendWise sync protocol primitives and backend adapters.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  crypto: ^3.0.7
  cryptography: 2.9.0
  domain:
    path: ../domain
  http: ^1.6.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
