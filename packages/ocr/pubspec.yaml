name: ocr
description: Text-recognition seam for SpendWise — engine-agnostic interface and value types.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.18.0
  web: ^1.1.1

dev_dependencies:
  test: ">=1.31.0 <1.31.2"
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - test/fixtures/
