name: domain
description: Pure Dart domain for SpendWise — models, LedgerState, accounting.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.0

dependencies:
  decimal: ^3.2.6
  uuid: ^4.6.0
  meta: ^1.18.0
  collection: ^1.19.0

dev_dependencies:
  test: ^1.31.2
  lints: ^6.1.0
